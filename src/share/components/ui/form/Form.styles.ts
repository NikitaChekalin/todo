import { createStyles } from '@mantine/core'

const useStyles = createStyles(() => ({
  content: {
    maxWidth: '600px',
    marginRight: 'auto',
    marginLeft: 'auto',
    paddingTop: '40px',
    paddingBottom: '40px',
  },
}))

export default useStyles
